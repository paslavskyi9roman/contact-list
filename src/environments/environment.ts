// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCVCR01JbajUhT8glmZR-j-D9IYZ5gXCMA',
    authDomain: 'contacts-ng-37a16.firebaseapp.com',
    projectId: 'contacts-ng-37a16',
    storageBucket: 'contacts-ng-37a16.appspot.com',
    messagingSenderId: '207163913452',
    appId: '1:207163913452:web:a0707ebe20cbe1125fa1c9',
    measurementId: 'G-W3LZF0NSSF'

  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
